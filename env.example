# PostgreSQL connection string
DATABASE_URL=postgres://username:password@localhost:5432/fcie_db

# Random secret string for session signing (generate with: openssl rand -hex 32)
SESSION_SECRET=replace-with-a-random-secret-string
